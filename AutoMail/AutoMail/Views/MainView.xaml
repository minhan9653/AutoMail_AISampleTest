<Window x:Class="AutoMail.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoMail.Views"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>

        <!-- ========================= -->
        <!-- ① 입력 영역 (좌측) -->
        <!-- ========================= -->
        <StackPanel Grid.Column="0" Margin="0,0,10,0">

            <TextBlock Text="업무 유형" FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding WorkTypes}"
                SelectedItem="{Binding SelectedWorkType}"
                Margin="0,5,0,10" />

            <TextBlock Text="대상" FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding Targets}"
                SelectedItem="{Binding SelectedTarget}"
                Margin="0,5,0,10" />

            <TextBlock Text="톤" FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding Tones}"
                SelectedItem="{Binding SelectedTone}"
                Margin="0,5,0,10" />

            <TextBlock Text="핵심 내용" FontWeight="Bold" />
            <TextBox Text="{Binding CoreContent}"
                     AcceptsReturn="True"
                     Height="150"
                     Margin="0,5,0,10"
                     VerticalScrollBarVisibility="Auto" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="📧 메일 생성"
                        Command="{Binding GenerateMailCommand}"
                        Margin="5" />
                <Button Content="📝 보고서 생성"
                        Command="{Binding GenerateReportCommand}"
                        Margin="5" />
                <Button Content="💬 회신 생성"
                        Command="{Binding GenerateReplyCommand}"
                        Margin="5" />
            </StackPanel>
        </StackPanel>

        <!-- ========================= -->
        <!-- ② 결과 영역 (우측) -->
        <!-- ========================= -->
        <StackPanel Grid.Column="1">
            <TextBlock Text="생성된 문서"
                       FontWeight="Bold"
                       Margin="0,0,0,5" />

            <TextBox Text="{Binding ResultText}"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" />
        </StackPanel>

    </Grid>
</Window>
